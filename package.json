{
  "name": "protobufjs-rslux",
  "version": "0.1.0",
  "description": "Rust-accelerated Protocol Buffers for JavaScript",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "protobuf-rslux",
    "triples": {
      "defaults": true
    }
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "test": "node test.js",
    "bench": "node benchmark.js",
    "bench:compare": "node benchmark-comparison.js"
  },
  "dependencies": {
    "@napi-rs/cli": "^2.0.0"
  },
  "devDependencies": {
    "protobufjs": "^7.0.0",
    "benchmark": "^2.1.4"
  }
}
